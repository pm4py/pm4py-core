import pyarrow


def apply(log, parameters=None):
    if parameters is None:
        parameters = {}


def export_to_file(log, file_path, parameters=None):
    if parameters is None:
        parameters = {}