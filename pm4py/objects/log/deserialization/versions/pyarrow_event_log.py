import pyarrow

def apply(bytes, parameters=None):
    if parameters is None:
        parameters = {}


def import_from_file(file_path, parameters=None):
    if parameters is None:
        parameters = {}
